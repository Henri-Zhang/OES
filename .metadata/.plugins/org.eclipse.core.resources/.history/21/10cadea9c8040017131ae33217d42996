AUI().use(
	'aui-io-request',<portlet:namesapce />
	function (A) {
		A.all('input[type="radio"]').on(
			'click',
			function(event) {
				var node = event.target;
				var value = node.get('value');
				var questionOrder = node.get('name');
				alert('haha');
			}
		);
	}
);