AUI().use(
	'aui-io-request',
	function (A) {
		var resourceURL = A.one('input[name=*resourceURL]').get('value');
		A.all('input[type="radio"]').on(
			'click',
			function(event) {
				var node = event.target;
				var value = node.get('value');
				var questionOrder = node.get('name');
				A.io.request(resourceURL, {
					
				});
			}
		);
	}
);