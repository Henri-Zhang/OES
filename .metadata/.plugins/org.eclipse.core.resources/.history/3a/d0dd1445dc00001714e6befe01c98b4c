AUI().use(
	'node',
	function(A) {
		A.one('.input-group-btn > button').on(
			'click', function(event) {
				var keyword = event.target.text();
				A.one()
				event.preventDefault();
				AUI().one('div.form-group.input-text-wrapper > input').set('value', keyword);
			}
		);
	}
);