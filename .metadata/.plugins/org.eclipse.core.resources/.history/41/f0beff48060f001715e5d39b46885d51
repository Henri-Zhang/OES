

<liferay-frontend:defineObjects />

<liferay-theme:defineObjects />

<portlet:defineObjects />

<c:choose>
    <c:when test="${isSignedIn }">
        You are signed in as <h3>${fullName }</h3>
    </c:when>
    <c:otherwise>

    </c:otherwise>
</c:choose>